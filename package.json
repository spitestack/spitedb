{
  "name": "spitestack",
  "private": true,
  "workspaces": [
    "packages/*",
    "crates/spitedb-napi"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "test": "bun run --filter '*' test"
  }
}
