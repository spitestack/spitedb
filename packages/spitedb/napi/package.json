{
  "name": "@spitestack/db",
  "version": "0.1.0",
  "description": "NAPI bindings for SpiteDB - event store with projections",
  "main": "index.js",
  "types": "index.d.ts",
  "exports": {
    ".": {
      "types": "./index.d.ts",
      "import": "./index.mjs",
      "require": "./index.js"
    }
  },
  "files": [
    "index.js",
    "index.mjs",
    "index.d.ts",
    "js/**/*.js",
    "js/**/*.d.ts"
  ],
  "napi": {
    "name": "spitedb",
    "triples": {
      "defaults": true,
      "additional": [
        "aarch64-apple-darwin",
        "x86_64-apple-darwin",
        "x86_64-unknown-linux-gnu",
        "aarch64-unknown-linux-gnu"
      ]
    }
  },
  "scripts": {
    "artifacts": "napi artifacts",
    "build": "rm -f *.node && napi build --platform --release && node scripts/copy-dylib.js && tsc -p tsconfig.build.json && node scripts/fix-types.js",
    "build:debug": "rm -f *.node && napi build --platform && node scripts/copy-dylib.js && tsc -p tsconfig.build.json && node scripts/fix-types.js",
    "prepublishOnly": "napi prepublish -t npm",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "universal": "napi universal"
  },
  "devDependencies": {
    "@napi-rs/cli": "^2.18.0",
    "@types/node": "^20.10.0",
    "bun-types": "^1.3.4",
    "msgpackr": "^1.11.8",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">= 18"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/spitedb/spitedb"
  },
  "license": "MIT",
  "keywords": [
    "event-sourcing",
    "event-store",
    "sqlite",
    "napi",
    "projections"
  ]
}
