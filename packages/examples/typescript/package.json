{
  "name": "spitestack-example",
  "type": "module",
  "scripts": {
    "spitestack": "cargo run -p spitestack-cli --bin spitestack --",
    "dev": "bun run spitestack dev",
    "build": "bun run spitestack compile",
    "check": "bun run spitestack check",
    "test": "bun test",
    "load-gen": "bun run scripts/load-gen.ts"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}
